{
  "name": "h-digest",
  "version": "0.1.0",
  "description": "small minimal quantile and cdf estimator for multiple large data streams",
  "keywords": [
    "quantiles",
    "median",
    "percentiles",
    "t-digest",
    "q-digest",
    "monte-carlo"
  ],
  "author": "Hugo Villeneuve",
  "main": "./h-digest.js",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "cotest": "^1.0.0",
    "tdigest": "^0.1.1"
  },
  "repository": {
		"type": "git",
		"url": "https://github.com/hville/h-digest.git"
	},
  "scripts": {
    "test": "node ./tst/h-digest",
    "benchmark": "node ./tst/benchmark",
    "prepublish": "npm test",
    "min": "uglifyjs ./h-digest.js -c unsafe,drop_console --screw-ie8 --preamble //hugov@runbox.com -m --wrap miniQuant -o ./index.min.js"
  },
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
